module.exports = function ({
    host,
    port,
    user,
    password,
    fileroute,
}) {
    this.host = host
    this.port = port
    this.user = user
    this.password = password
    this.fileroute = fileroute
}